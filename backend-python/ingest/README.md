DEPRECATED: Python ingestion starters

This folder previously contained Python ingestion scripts. The ingestion
pipeline has been migrated to `backend-rust/`. Keep this README only for
historical reference.
